# main.tf
# Intentionally minimal. All resources & providers live in the other *.tf files.
# Keeping this file as a stub avoids duplicate definitions.

# (Optional) keep shared locals here if you want
locals {
  project_name = var.project_name
}
