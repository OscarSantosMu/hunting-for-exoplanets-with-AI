FROM python:3.11-slim

WORKDIR /app
COPY pyproject.toml requirements.txt ./
RUN pip install -U pip
COPY . .
RUN pip install -e .[api]

CMD ["gunicorn", "exoplanet_ai.api.app:app", "--host", "0.0.0.0", "--port", "8000"]
